import { Bell, Menu, Search } from 'lucide-react'
import { useAppStore } from '../../store/useAppStore'
import type { Pestana } from '../../types'
interface Props { pestana: Pestana; abrirMenu: () => void }
export function Header({ pestana, abrirMenu }: Props) { const actualizarFiltros = useAppStore((state) => state.actualizarFiltros); return <header className="flex items-center justify-between border-b border-slate-200 bg-white px-5 py-4 lg:px-8"><div className="flex items-center gap-3"><button onClick={abrirMenu} className="text-slate-500 lg:hidden" aria-label="Abrir navegación"><Menu size={21} /></button><div><p className="text-xs font-semibold text-slate-400">Miércoles, 26 de agosto de 2026</p><h1 className="font-display text-xl font-bold text-tinta">{pestana}</h1></div></div><div className="flex items-center gap-3"><label className="hidden items-center gap-2 rounded-lg bg-papel px-3 py-2 text-slate-400 md:flex"><Search size={16} /><input aria-label="Buscar" onChange={(event) => actualizarFiltros({ termino: event.target.value })} className="w-36 bg-transparent text-sm outline-none placeholder:text-slate-400" placeholder="Buscar..." /></label><button aria-label="Notificaciones" className="relative rounded-lg p-2 text-slate-500 hover:bg-papel"><Bell size={19} /><span className="absolute right-1 top-1 h-1.5 w-1.5 rounded-full bg-coral" /></button></div></header> }