import { Boxes, CircleDollarSign, LayoutDashboard, Settings, Users, X } from 'lucide-react'
import type { Pestana } from '../../types'
interface Props { activa: Pestana; cambiar: (pestana: Pestana) => void; abierta: boolean; cerrar: () => void }
export function Sidebar({ activa, cambiar, abierta, cerrar }: Props) { const items = [{ nombre: 'Resumen', icono: LayoutDashboard }, { nombre: 'Productos', icono: Boxes }, { nombre: 'Ventas', icono: CircleDollarSign }, { nombre: 'Clientes', icono: Users }] as const; return <aside className={`${abierta ? 'translate-x-0' : '-translate-x-full'} fixed inset-y-0 left-0 z-40 flex w-64 flex-col border-r border-slate-200 bg-tinta p-5 transition-transform lg:static lg:translate-x-0`}><div className="flex items-center justify-between px-2 py-3"><div className="flex items-center gap-2.5 text-white"><div className="grid h-9 w-9 place-items-center rounded-lg bg-coral font-display text-lg font-bold">N</div><span className="font-display text-xl font-bold tracking-tight">northstar</span></div><button onClick={cerrar} className="text-slate-400 lg:hidden" aria-label="Cerrar navegación"><X size={20} /></button></div><p className="mb-3 mt-10 px-3 text-[10px] font-bold uppercase tracking-[.18em] text-slate-500">Workspace</p><nav className="space-y-1">{items.map(({ nombre, icono: Icono }) => <button key={nombre} onClick={() => { cambiar(nombre); cerrar() }} className={`flex w-full items-center gap-3 rounded-lg px-3 py-3 text-sm font-semibold transition ${activa === nombre ? 'bg-white/10 text-white' : 'text-slate-400 hover:bg-white/5 hover:text-white'}`}><Icono size={18} />{nombre}</button>)}</nav><div className="mt-auto border-t border-white/10 pt-4"><button className="flex w-full items-center gap-3 px-3 py-3 text-sm font-semibold text-slate-400 hover:text-white"><Settings size={18} />Configuración</button><div className="mt-4 flex items-center gap-3 rounded-lg bg-white/5 p-3"><div className="grid h-8 w-8 place-items-center rounded-full bg-[#e7c9a8] text-xs font-bold text-tinta">AM</div><div><p className="text-xs font-bold text-white">Ana Martínez</p><p className="text-[10px] text-slate-400">Administradora</p></div></div></div></aside> }